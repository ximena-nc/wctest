# Webchat
Plugin for websites

## Develop branch:

  ```cmd 
  git checkout develop
  ``` 

## Install

  ```cmd
  bower install firebase --save
  sudo npm install -g firebase-tools
  firebase login
  ```

## Execute

  ```cmd
  firebase serve
  ```

## Deploy

  ```cmd
  firebase deploy
  ```